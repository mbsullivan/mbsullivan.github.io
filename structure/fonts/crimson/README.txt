Folder names are derived from the descriptive labels in the input zip filenames.

Within each folder, the filename has one of the following (style, code)
appendages:

("semibolditalic", "sbi"), ("bolditalic", "bi"), ("semibold", "sb"), 
("roman", "r"), ("italic", "i"), ("bold", "b")
